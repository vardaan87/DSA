// Bitwise Operator

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a = 6, b = 10;

    // Bitwise & Operator
    cout << (a & b) << endl;

    // Bitwise | Operator
    cout << (a | b) << endl;

    // Bitwise ^ Operator
    cout << (a ^ b) << endl;

    return 0;
}